[Intro] : image reader
creating a image reader using a Java.bufferImage tool.
Only Work for black and white image. What it do? Read Pixel from images and store 0(Black) and 1(White) in 2D Array. Create Text Image out of it.
